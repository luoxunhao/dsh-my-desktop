# 安装包、运行时和构建输出只保留在本地或 GitHub Release，不提交源码仓库。
# 全部锚定到仓库根：这些规则针对的是根目录下的产物目录，若不加前导斜杠，
# 一条 `runtime/` 会匹配任意层级的同名目录 —— 它曾经把源码目录 src/runtime/
# 一并命中，导致该目录下新增文件被静默忽略（已跟踪的旧文件不受影响，
# 所以问题只在「新建文件」时才暴露）。新增同类规则时保持这个风格。
/dist/
/release/
/runtime/
/runtime-node/
/runtime-plugins/
node_modules/

# 开发过程产物仅保留在本地。
/coverage/
*.log
artifacts/
/output/

# 项目文档仅在本地维护，不纳入 Git 仓库。
/docs/

runtime-dsh/
runtime-dsh.tgz
runtime-dsh.tgz.sha256
.codegraph/
_tmp*
System.Management.Automation.Internal.Host.InternalHost

# 本地构建用固定 Node 运行时（避免每次下载），不入库。
.build-node/

# pnpm 本地内容寻址存储（本机缓存，不入库）。
.pnpm-store/

# .scratch/ 是本地临时工作区，**有意只挑着提交**：里面既有要入库的设计草稿
# （.scratch/recovery-ui-alignment/、.scratch/refactor-src/ 下的 .md 与探针脚本），
# 也有一次性实验留下的构建副产物。所以这里只按「副产物」形态忽略，不整目录忽略。
#
# seed-probe 曾以 120 MB / 2148 个文件形式躺在未跟踪区（内含一整套 pnpm store
# 缓存），`git add -A` 会把它整个灌进仓库。忽略这几类即可，探针脚本本身仍可入库。
.scratch/**/store/
.scratch/**/node_modules/
.scratch/**/*.tgz
.scratch/**/pkg/


# 上游 dshmarket 参考源码，非本仓库构建单元：随包预装走 npm 下载
# dshmarket@<version>（见 pnpm-workspace.yaml 里同一处决策与 prepare-runtime.ts 的
# stageBundledPlugins），仓库内零引用。
#
# 之所以要 ignore 而不是放着不管：该目录自带 .git，一旦顺手 `git add` 就会被记成
# 一个 gitlink（模式 160000，只存 commit 指针、不存内容），而根仓库没有 .gitmodules，
# 克隆者会拿到一个空目录且无从补回 —— 构建在毫不相关的地方失败。
# 需要阅读上游实现时直接看 GitHub，或临时 clone 到本路径。
/plugins/dsh-market/

/_recover_f81081b4
