{
  "//": "Recovery-page UI sources. Separate from the root tsconfig because they need JSX and DOM libs, while the launcher main process must NOT have DOM types (it would let Electron main code call `document` and compile).",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx",
    "types": [],
    "noEmit": true
  },
  "include": ["src/recovery-ui/**/*.ts", "src/recovery-ui/**/*.tsx"]
}
