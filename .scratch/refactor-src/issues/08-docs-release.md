# 08 — 文档收尾 + 发版 0.1.4

**What to build:** 把重构后的**实际**结构记录成项目文档，并发布一个带明确回退点的版本。

**为什么文档放最后**：它要记录实际发生的结构，而不是计划中的结构。07 与 13 都允许拆不干净，
所以最终形态只有做完才知道。

**为什么单独一个 ticket**：与 07 分开，使得无论 IPC 拆到什么程度，文档与发版都能独立完成——
避免出现"拆到一半要不要发版"的尴尬。

**Blocked by:** 13 — 抽出恢复流程（**注意：依赖链已从 07 延长到 13**，见下方说明）

**Status:** ready-for-agent

- [ ] 写 `CONTEXT.md`（仓库根）：记录本次澄清的领域术语，例如可变状态 store、晚绑定、
      启动服务、窗口注册表、窄接口签名、组合根。术语表只放本项目特有的概念，不放通用编程概念
- [ ] 写 ADR（`adr/`，**不是** `docs/adr/`——该目录被 gitignore），至少包含：
      「为何用依赖注入而非事件总线」（ticket 06/13 都用到这个决策）
- [ ] 更新 `AGENTS.md` 的「关键文件」章节为新路径与新的模块边界
- [ ] 更新 `AGENTS.md` 的测试基线数字
- [ ] 版本升到 0.1.4（启动器与随包插件同步）
- [ ] `dist-local` 出包成功，产物文件名带 0.1.4
- [ ] 提交并打 annotated tag `v0.1.4`
- [ ] 确认最终验收（**已修正，见下**）

## 依赖链变更说明

本 ticket 原定 blocked by 07。补 ticket 后依赖链延长为：

```
06 ─▶ 07 ─▶ 13 ─▶ 08
```

因为 13（恢复流程）会移动 `installRecoveryIpc`，而 08 要记录最终结构——若在 13 之前发版，
文档会立刻过期。

## 验收标准修正（重要）

原标准写的是「主入口 < 300 行」。**实测证明该目标不成立**：补完 09~13 后预计仍有约
725 行，而那些是 `startApplication`(172) + `handleDshIpc`(48) + `recycleDshForPluginUpdate`(43)
+ `handleRendererBootReport`(48) + 各类启动/错误/消息 helper + 薄包装。

`startApplication` 内部是 10 段**有意义**的编排步骤（加载偏好 → 建 store → 绑定注册表 →
解析路径 → 解压 → 补种 → 启动 → 打开界面 → 写 smoke 文件 → 排更新检查），不是可清理的杂波。

**修正后的验收标准**：

- [ ] `src/` 无平铺文件；每个文件可从目录判断所属层
- [ ] 主入口**只做编排**：不承载任何具体功能域的实现（通知/更新/托盘/IPC/恢复/终端/
      profile/对话框/主题均已独立）
- [ ] 无模块级可变绑定残留（除 `state` 及其注册表引用）
- [ ] 跨层依赖单向（用依赖图脚本复核无反向边）
- [ ] 各模块使用窄接口签名，不整包接收 `DesktopState`
- [ ] 测试基线不退化；晚绑定护栏通过
- [ ] 记录最终行数，但**不以某个具体数字为验收条件**——若残留明显超出预期，
      在文档中说明残留构成与原因

**备注**：若 07 或 13 未能拆干净，在 ADR 或 `AGENTS.md` 中记录"主入口仍承载部分 X"这一现状
及原因，避免后续 agent 误以为它是遗漏。

